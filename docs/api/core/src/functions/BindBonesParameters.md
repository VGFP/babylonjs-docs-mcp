[**@babylonjs/root v1.0.0**](../../../README.md)

***

[@babylonjs/root](../../../README.md) / [core/src](../README.md) / BindBonesParameters

# Function: BindBonesParameters()

> **BindBonesParameters**(`mesh?`, `effect?`, `prePassConfiguration?`): `void`

Defined in: [packages/dev/core/src/Materials/materialHelper.functions.ts:449](https://github.com/BabylonJS/Babylon.js/blob/8fc9bce8645980fefc18c7550fb28469d6da3a62/packages/dev/core/src/Materials/materialHelper.functions.ts#L449)

Binds the bones information from the mesh to the effect.

## Parameters

### mesh?

[`AbstractMesh`](../classes/AbstractMesh.md)

The mesh we are binding the information to render

### effect?

[`Effect`](../classes/Effect.md)

The effect we are binding the data to

### prePassConfiguration?

`PrePassConfiguration`

Configuration for the prepass, in case prepass is activated

## Returns

`void`
